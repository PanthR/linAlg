var Matrix = require('../../linAlg/matrix');
var DiagS = Matrix.Solver.DiagS;
var expect = require('chai').expect;

describe('Diagonal solver', function() {
   it('works for non-zero entries and vector RHS', function() {
      
   });
   it('works for non-zero entries and matrix RHS', function() {
      
   });
});
